# Chatbot Used
Claude

# Prompts

## 1
como desarrolador javaScript, crea una pagina web con un html y su correspondiente script JS. Debe cumplir todos estos requisitos:

* El objetivo es invertir un campo de texto dado por el usuario manualmente. Ejemplo: si introduzco `AI4Devs` devuelve `sveD4IA`
* Una caja donde el usuario puede introducir el texto que sera invertido
* Un boton reverse. El resultado solo debe mostrarse cuando presionamos el boton con el texto "Reverse"
* Un campo de salida, donde debe mostrarse el resultado de invertir la cadena. Dicho texto tiene un valor inicial en blanco
* Un boton copiar que copiara en el portapapeles el resultado mostrado en el campo de salida.
* Como base partimos de este html: index.html

```
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reverse String</title>    
</head>
<body>
<script src="script.js"></script>
</body>
</html>

```

* script.js debe contener toda la logica.
* La estetica del resultado final debe ser igual al mostrado en la imagen que te adjunto

## 2
Cambia al rol de revisor de PR. Revisa los dos ficheros y corrige todo lo que este mal, informame de los cambios y el por que